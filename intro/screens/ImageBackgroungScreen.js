import{ Text, StyleSheet, View, Button} from 'react-native'

export default function ImageBackgroungScreen(){
return (
    <view>
        <text>Proximamente por Guillermo y equipo</text>
    </view>
    )
}
