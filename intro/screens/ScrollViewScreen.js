import{ Text, StyleSheet, View, Button} from 'react-native'

export default function ScrollViewScreen(){
return (
    <view>
        <text>Proximamente por Barbara y equipo</text>
    </view>
    )
}
