import{ Text, StyleSheet, View, Button} from 'react-native'

export default function ActivityIndicatorScreen(){
return (
    <view>
        <text>Proximamente por Oscar y equipo</text>
    </view>
    )
}